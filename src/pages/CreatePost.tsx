import React from "react";
import { Editor } from "../components/editor/ReactQuill";

const CreatePost = () => {
    return (
        <div>
            <Editor />
        </div>
    );
};

export default CreatePost;
